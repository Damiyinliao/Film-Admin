// 首页地址（默认）
export const HOME_URL: string = "/home/index";
// 登录地址（默认）
export const LOGIN_URL: string = "/login";

// 默认主题颜色
export const DEFAULT_THEME_COLOR: string = "#009688";

// 默认主题颜色
export const DEFAULT_PRIMARY: string = "#009688";

// 路由白名单地址(必须是本地存在的路由 staticRouters.ts 中的路由)
export const ROUTER_WHITE_LIST: string[] = ["/login", "/404", "/403", "/500"];
